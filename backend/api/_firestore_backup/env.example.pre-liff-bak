# Booking API configuration
PORT=4000

# MySQL / MariaDB connection (local dev, or a cloud MySQL provider —
# Railway, Clever Cloud, Aiven, AWS RDS, Google Cloud SQL, etc.)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=nail_booking
