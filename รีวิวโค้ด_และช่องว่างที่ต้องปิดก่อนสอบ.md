# รีวิวโค้ดจริง และช่องว่างที่ต้องปิดก่อนสอบ
ระบบจองคิวและจัดการให้บริการทำเล็บออนไลน์ (Take Care Nail)
ตรวจเมื่อ 11 ส.ค. 2569 · จัดลำดับตามความเร่งด่วน

---

## 🔴 เร่งด่วนที่สุด — ความปลอดภัย (ทำวันนี้)

### 1. ไฟล์กุญแจลับ `serviceAccountKey.json` ถูกเก็บไว้ในโปรเจกต์
- ไฟล์ `firebase/serviceAccountKey.json` เป็น **service account จริง** ของโปรเจกต์ `online-naile-service` และมี `private_key` อยู่ข้างใน
- ถ้าไฟล์นี้หลุด (อัปโหลด GitHub / แนบส่ง) ผู้อื่นจะเข้าถึงฐานข้อมูลได้ทั้งหมดในสิทธิ์ผู้ดูแล
- **ต้องทำ:**
  1. ลบไฟล์ออกจากโฟลเดอร์ที่จะแชร์/อัปโหลด และเพิ่มลง `.gitignore` (`serviceAccountKey.json`, `.env`)
  2. **เพิกถอน (revoke) กุญแจเดิมแล้วสร้างใหม่** ที่ Google Cloud Console → IAM → Service Accounts → Keys (ถือว่ากุญแจเดิมรั่วแล้ว)
  3. โหลดกุญแจจาก path ที่อยู่นอก repo หรือจาก environment variable แทน
- พบ `backend/.git` เป็น git repo ซ้อนอยู่ด้วย — ตรวจว่าไฟล์ลับไม่เคยถูก commit (ถ้าเคย ต้องลบทั้งประวัติ)

---

## 🟠 สำคัญมาก — ช่องว่างระหว่าง "เอกสารที่ออกแบบ" กับ "โค้ดที่ทำจริง"

> คณะกรรมการมักขอให้เดโมระบบที่ทำงานจริง จุดนี้คือความเสี่ยงหลัก ควรตัดสินใจว่าจะ (ก) เขียนโค้ดส่วนที่ขาดให้รันได้ หรือ (ข) ระบุในเล่มให้ชัดว่าส่วนใดเป็นต้นแบบ (prototype)

### 2. Backend ยังไม่มีตัวระบบจองจริง
- โฟลเดอร์ `backend/` มีแค่ `line_webhook.js` (บอทตอบเมนู LINE) เท่านั้น
- **อัลกอริทึมคำนวณ slot** และ **การจองแบบ transaction กันคิวชน** ที่เขียนไว้ในเล่ม/ในไฟล์ `firestore_data_model.md` และ `liff_auth_flow.md` — **ยังเป็นแค่โค้ดตัวอย่างในเอกสาร ยังไม่ได้ทำเป็นเซิร์ฟเวอร์ที่รันได้**
- ยังไม่มี Express server ที่เปิด endpoint จริง เช่น `POST /api/auth/line`, `GET /api/slots`, `POST /api/bookings`
- **แนะนำ:** สร้าง `backend/server.js` (Express) ที่มีอย่างน้อย 3 endpoint นี้ ต่อ Firestore ผ่าน Admin SDK — นี่คือ "แก่น" ที่ทำให้โปรเจกต์เป็นงานวิศวกรรม ควรมีให้เดโมได้

### 3. หน้าแอดมิน (nail-booking-admin) ยังเป็นต้นแบบ UI ใช้ข้อมูลจำลอง
- ทุกหน้าใน `src/pages/*` `import` ข้อมูลจาก `src/data/mockData.js` — **ยังไม่ได้ต่อ Firestore จริง** (แพ็กเกจ `firebase` ยังไม่ได้ติดตั้งในฝั่ง frontend เลย)
- **แนะนำ:** ติดตั้ง `firebase` แล้วแทน mock ด้วยการอ่านจริง (`getDocs`/`onSnapshot`) อย่างน้อยหน้า Dashboard / Bookings / Services เพื่อเดโม
- ยังไม่พบ **หน้าจองฝั่งลูกค้า (LIFF)** ในโค้ด — มีแต่หน้าแอดมิน ทั้งที่ flow การจองของลูกค้าคือหัวใจ ควรมีหน้า LIFF อย่างน้อย 1 หน้า (เลือกบริการ → เลือกวัน/ช่าง → เห็น slot → ยืนยัน)

### 4. ข้อมูลบริการไม่ตรงกันระหว่างไฟล์ (แหล่งข้อมูลซ้ำซ้อน)
ราคา/ระยะเวลา/หมวด ของบริการเดียวกันไม่ตรงกันใน 3 ที่ ถ้าเดโมแล้วเลขไม่ตรงเล่มจะโดนถาม:

| บริการ | `seed_firestore.js` | `mockData.js` | `line_webhook.js` |
|---|---|---|---|
| ทาสีเจล | 300 บาท / 60 นาที / หมวด "พื้นฐาน" | 350 บาท / หมวด "ทาสี" | 300 บาท / 60 นาที |
| ต่อเล็บเจล | 700 / 120 / buffer 15 | 700 / 120 / buffer 15 | 700 / 120 |
| เพ้นท์ลวดลาย | 200 / 45 | 200 / 45 | 200 / 45 |

- **แนะนำ:** ยึด **Firestore เป็นแหล่งข้อมูลจริงเดียว** แล้วให้ mockData และข้อความในบอทอ้างอิงชุดเดียวกัน (บอทควรอ่านจาก Firestore ถ้าทำได้ หรืออย่างน้อยแก้ตัวเลขให้ตรง)

---

## 🟢 จุดที่ทำได้ดีแล้ว (ใช้ตอบกรรมการได้เต็มปาก)

1. **`firestore.rules` ออกแบบถูกหลัก** — ปิด `write` ฝั่ง client ของ `bookings/users/notifications` ทั้งหมด บังคับให้การจองผ่าน backend เท่านั้น เพื่อคุม transaction กันคิวชน (เป็นแนวทางที่ถูกต้องเรื่อง data integrity)
2. **LIFF auth ตรวจ token ฝั่งเซิร์ฟเวอร์** — ไม่เชื่อ `lineUserId` จาก client ตรง ๆ แต่ verify access token กับ LINE ก่อน (`client_id` ตรง channel เรา) แล้วค่อยเชื่อถือ เป็นการกันการปลอมตัวตนที่ถูกต้อง
3. **Webhook ใช้ `@line/bot-sdk` v8 middleware** ตรวจ signature อัตโนมัติ และไม่วาง `express.json()` ก่อน middleware (ถ้าวางจะตรวจ signature พัง) — ทำถูกแล้ว
4. **โมเดลข้อมูลออกแบบดี** — ใช้ embed (`items[]`, `result{}`, `schedule{}`) และ price/duration snapshot อย่างเหมาะสม เข้าใจหลัก NoSQL

---

## 🟡 งานบ้านเล็ก ๆ (เก็บกวาดก่อนส่ง)
- ลบไฟล์ขยะที่หลุดเข้ามา: `vite.config.js.timestamp-*.mjs` หลายไฟล์, `.DS_Store` ทุกโฟลเดอร์
- `passwordHash` ของแอดมินใน seed ยังเป็น `$2b$10$REPLACE_WITH_REAL_BCRYPT_HASH` — ต้องสร้าง hash จริงด้วย bcrypt ก่อนใช้งาน
- LIFF เก็บ `sessionToken` ใน `localStorage` — ใช้ได้ แต่ควรรู้ข้อจำกัดเรื่อง XSS (ตอบกรรมการได้ว่าเป็น trade-off ของ SPA)

---

## ลำดับที่ควรลงมือ (ก่อนสอบ)
1. 🔴 ลบ + เพิกถอน `serviceAccountKey.json` (ความปลอดภัย) — เดี๋ยวนี้
2. 🟠 เขียน `backend/server.js` ให้มี auth + slot + booking(transaction) ที่รันได้จริง — แก่นของงาน
3. 🟠 ต่อหน้าแอดมินอย่างน้อย 2–3 หน้าเข้ากับ Firestore จริง + ทำหน้า LIFF จองของลูกค้า 1 หน้า
4. 🟠 รวมข้อมูลบริการให้เหลือแหล่งเดียว (Firestore) เลขตรงกับเล่ม
5. 🟡 เก็บกวาดไฟล์ขยะ + สร้าง bcrypt hash จริง
