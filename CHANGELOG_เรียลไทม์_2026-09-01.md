# เพิ่มระบบอัปเดตแบบเรียลไทม์ (ไม่ต้องกดรีเฟรช) — 1 กันยายน 2569

ตามที่ขอไว้ ทำให้ทั้งหน้าแอดมินและหน้าลูกค้าอัปเดตข้อมูลทันทีโดยไม่ต้องกดรีเฟรช โดยใช้ **WebSocket (Socket.IO)** — เปลี่ยนจากการ "poll" (ดึงข้อมูลใหม่ทุก 8 วินาที) เป็นให้ **backend ส่งข้อมูลใหม่มาหาเองทันทีที่มีการเปลี่ยนแปลง**

## หลักการทำงาน

Backend (`server.js`) เปิด Socket.IO คู่ไปกับ REST API เดิม (คนละกลไก แต่ใช้พอร์ตเดียวกัน) แบ่งเป็น "ห้อง" (room):

- ห้อง `admin` — แดชบอร์ดแอดมิน เห็นการจองของลูกค้าทุกคน
- ห้อง `customer:<id>` — ลูกค้าแต่ละคนเห็นเฉพาะการจองของตัวเอง (**แยกห้องตามตัวตนจริง ป้องกันข้อมูลลูกค้าคนอื่นหลุด** — ทดสอบแล้วว่าห้องแยกกันจริง)

ทุกครั้งที่มีการเปลี่ยนแปลงข้อมูล (จองใหม่, ยกเลิก, แอดมินแก้สถานะ, แก้บริการ/ช่าง/วันหยุด/การตั้งค่า) เซิร์ฟเวอร์จะส่งข้อมูลชุดใหม่ไปหาทุกคนที่เกี่ยวข้องทันที ไม่ต้องรอให้ฝั่ง client ถามเข้ามา

**สิ่งที่จะเกิดขึ้นแบบเรียลไทม์:**

- แอดมินแก้ไข/เพิ่ม/ลบ บริการ, ช่าง, วันหยุด, การตั้งค่าร้าน → ลูกค้าที่เปิดแอปอยู่เห็นการเปลี่ยนแปลงทันที
- ลูกค้าจองคิว → แอดมินเห็นรายการจองใหม่ขึ้นในแดชบอร์ดทันที (รวมถึงกระดิ่งแจ้งเตือน)
- แอดมินเปลี่ยนสถานะการจอง (ยืนยัน/ยกเลิก/เสร็จสิ้น) → ลูกค้าเจ้าของการจองเห็นการเปลี่ยนแปลงทันที
- **ป้องกันคิวชนซ้ำซ้อนเชิง UX**: ถ้าลูกค้า A กำลังเลือกเวลาอยู่ แล้วลูกค้า B จองเวลาเดียวกันไปก่อน ช่องเวลานั้นจะเทาลง (ไม่ว่างแล้ว) ทันทีในหน้าจอของลูกค้า A โดยอัตโนมัติ และถ้า A บังเอิญเลือกเวลานั้นค้างไว้พอดี ระบบจะเคลียร์ตัวเลือกให้พร้อมข้อความแจ้งเตือน ("ช่วงเวลาที่เลือกเพิ่งถูกจองไปแล้ว")

## ไฟล์ที่เพิ่ม/แก้ไข

**Backend:**
- `backend/api/server.js` — เพิ่ม Socket.IO server, ห้อง admin/customer, ส่ง event หลัง mutation ทุกจุด (services, staff, closures, settings, bookings)
- `backend/api/lib/repo.js` — เพิ่มฟังก์ชัน `getUserExternalId()` (แก้ปัญหาที่พบระหว่างทดสอบ: ลูกค้าโหมดทดลอง/มือถือใช้ id คนละแบบกับ id ภายในฐานข้อมูล ต้องแปลงไปมาให้ห้อง Socket ตรงกันเสมอ)
- `backend/api/package.json` — เพิ่ม `socket.io`

**หน้าแอดมิน (`nail-booking-frontend`):**
- `src/lib/socket.js` (ใหม่) — การเชื่อมต่อ Socket.IO
- `src/context/DataContext.jsx` — เปลี่ยนจาก poll ทุก 8 วิ เป็นรับข้อมูลผ่าน socket ทั้งหมด (bookings, services, staff, closures, settings)
- `package.json` — เพิ่ม `socket.io-client`

**หน้าลูกค้า (`nail-booking-customer`):**
- `src/lib/socket.js` (ใหม่) — การเชื่อมต่อ Socket.IO
- `src/context/BookingContext.jsx` — รับข้อมูล catalog + การจองของตัวเองผ่าน socket แทน poll, เพิ่ม `availabilityVersion` ให้หน้าเลือกเวลารีเฟรชอัตโนมัติ
- `src/pages/booking/BookDateTime.jsx` — ฟังสัญญาณ `availabilityVersion` แล้วโหลดคิวว่างใหม่ทันที พร้อมเคลียร์เวลาที่เลือกไว้ถ้าถูกจองไปแล้ว
- `package.json` — เพิ่ม `socket.io-client`

หมายเหตุ: ถ้าไม่ได้ตั้งค่า `VITE_API_URL` (โหมด mock/ทดลองล้วน ๆ ไม่มี backend) ระบบจะข้ามการเชื่อมต่อ Socket.IO ไปเองโดยอัตโนมัติ ไม่กระทบการทำงานของโหมดสาธิต

## ทดสอบแล้ว

เขียน client script จำลอง 3 การเชื่อมต่อ (แอดมิน 1, ลูกค้าเจ้าของการจอง 1, ลูกค้าคนอื่นอีก 1) ยิง REST endpoint จริงกับ MariaDB ที่รันจริง แล้วตรวจว่า:

- แอดมินได้รับ `services:updated`, `bookings:admin:updated`, `availability:changed` ✅
- ลูกค้าเจ้าของการจองได้รับ `bookings:mine:updated` ✅
- ลูกค้าอีกคนที่ไม่เกี่ยวข้อง **ไม่ได้รับ** ข้อมูลการจองนี้เลย (ห้องแยกกันจริง ไม่มีข้อมูลรั่ว) ✅

ทุกไฟล์ผ่าน syntax check (`node --check`, esbuild parse สำหรับ JSX) ก่อนส่งขึ้นเครื่องคุณ และตรวจ md5 ตรงกันหมดหลัง deploy ไฟล์เดิมทุกไฟล์มีสำรองไว้ที่ `backend/api/_firestore_backup/` (ชื่อลงท้าย `.pre-realtime-bak`)

## สิ่งที่คุณต้องทำต่อ

1. รัน `npm install` ในทั้ง 3 โฟลเดอร์ (`backend/api`, `nail-booking-frontend`, `nail-booking-customer`) เพื่อติดตั้ง `socket.io` / `socket.io-client` — ผมลองรันผ่าน remote bridge แล้วแต่ช้ามากบนโฟลเดอร์ Windows (ไม่จบใน 3 นาที) แนะนำให้รันเองในเครื่องโดยตรง
2. รีสตาร์ท backend (`npm run start` หรือ `npm run dev`) หลังติดตั้งเสร็จ แล้วเปิดหน้าแอดมินคู่กับหน้าลูกค้า ลองแก้ไขข้อมูลฝั่งหนึ่งดูว่าอีกฝั่งอัปเดตทันทีโดยไม่ต้องรีเฟรชหรือไม่
3. ถ้า deploy จริงขึ้น hosting ที่ต่างโดเมนกัน (frontend กับ backend คนละที่) ให้ตรวจสอบว่า CORS ของ Socket.IO (ตอนนี้ตั้งเป็น `origin: '*'` ไว้กว้างเพื่อความง่ายในการทดสอบ) เหมาะกับสภาพแวดล้อมจริงหรือควรจำกัด origin ให้แคบลง
